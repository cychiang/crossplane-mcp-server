[project]
name = "crossplane-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ruff>=0.11.6",
    "mcp[cli]>=1.6.0",
    "kubernetes>=32.0.1",
]

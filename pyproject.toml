[project]
name = "crossplane-mcp-server"
version = "0.1.0"
description = "A Python implementation of the Model Context Protocol (MCP) for Crossplane."
authors = [
    { name = "Your Name", email = "your@email.com" },
]
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ruff>=0.11.6",
    "mcp[cli]>=1.8.0",
    "kubernetes>=32.0.1",
]
